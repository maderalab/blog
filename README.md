A static markdown blog. Each folder of `.md` files in `docs/` is a collection card; each file inside is an article. An optional `cover.*` image gives the card a picture.

**Deploy:** push to GitHub → Settings ▸ Pages ▸ Source = *GitHub Actions*.
Every push rebuilds and redeploys automatically.
